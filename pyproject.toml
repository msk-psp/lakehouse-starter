[project]
name = "lakestart"
version = "0.1.0"
description = "Self-hosted Iceberg lakehouse starter"
requires-python = ">=3.9"
dependencies = [
    "duckdb>=1.1",
]

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
